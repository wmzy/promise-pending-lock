{
  "name": "promise-pending-lock",
  "version": "0.1.0",
  "description": "Call an async function multi before the first resolve, only run once and share the result.",
  "main": "index.js",
  "scripts": {
    "test": "mocha -r should -r should-sinon"
  },
  "keywords": ["promise", "lock"],
  "author": "wmzy",
  "repository": {
    "type": "git",
    "url": "https://github.com/wmzy/promise-pending-lock.git"
  },
  "license": "ISC",
  "devDependencies": {
    "mocha": "^4.0.1",
    "should": "^13.1.3",
    "should-sinon": "0.0.6",
    "sinon": "^4.1.2"
  },
  "dependencies": {
    "lodash": "^4.17.4"
  }
}
